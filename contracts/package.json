{
  "name": "gamba-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Gamba multi-chain betting platform",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:ethereum": "hardhat run scripts/deploy.js --network ethereum",
    "deploy:bsc": "hardhat run scripts/deploy.js --network bsc",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}
